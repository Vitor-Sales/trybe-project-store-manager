const productModel = require('./product.model');
const saleModel = require('./saleModel');
const productSaleModel = require('./productSale.model');

module.exports = {
  productModel,
  saleModel,
  productSaleModel,
};